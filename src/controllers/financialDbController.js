const b1Controller = require("./b1Controller");

// This controller was a temporary mock for SAP operations.
// Restoring it to use the actual b1Controller for testing as requested.
module.exports = b1Controller;
